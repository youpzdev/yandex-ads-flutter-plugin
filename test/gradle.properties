kotlin.code.style=official
# Ограничиваем воркеров, чтобы фаза сборки тестов не разворачивалась на все ядра хоста
# и не накладывалась пиком на запущенные эмуляторы (см. UI-tests CPU budget).
org.gradle.workers.max=4
